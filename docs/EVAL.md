# Evaluation results

Two numbers, and the second is the one that matters.

**Turnout refuses 14 of 14 adversarial cases, and wrongly refuses 0 of 6 legitimate ones.** Forged
mutual aid requests, texts to somebody who sent STOP, asks past a volunteer's weekly limit, a
scoring request naming a role that does not exist. Refusing all of those is easy: a system that
refuses everything scores 100 percent. The control is the six things it must not refuse, and a
false refusal there is worse than the failure it was guarding against, because a coverage window
that is wrongly declined stays open with nobody looking at it again.

**Against the obvious alternative, it catches 3.5 times as many coverage failures.** The thing a
reasonable engineer builds instead is a roster check: can the people who marked themselves available
form a legal crew. Over 2000 simulated windows it caught 129 of 914 exposed windows. Turnout caught
446 of the same 914, including 317 the roster check called covered.

**Reply parsing reads 82 phrasings correctly, 100 percent, with no model call.** Those 82 are ones
I wrote. On 500 real text messages from the UCI SMS Spam Collection, none of which is a reply to a
coverage poll, **the parser correctly declined to interpret 98 percent** and handed them to the
model rather than guessing.

That second figure was 92.6 percent before it was measured, and finding out why fixed two real
defects in the parser.

Both are regenerated by scripts in this repository and run in CI on every push.

<!-- baseline:start -->
## Against the obvious alternative

Generated 2026-09-09 19:42 UTC by `python -m evals.baseline_eval`.

The alternative to all of this is a roster check: look at who marked themselves available, see whether those people can form a legal crew, and call the window covered if they can. Every volunteer scheduling tool does that. It is the incumbent, not a strawman.

A member marking themselves available is saying they are not at work. It is not a promise to turn out, and published response rates for volunteer first responders run between 17 and 47 percent. So four available people can be a legal crew on paper and no crew at all at two on a Tuesday.

Over **2000 simulated windows** for a 14 member department, drawn against each member's own response history, **914 could not have raised a legal crew** if a call had come.

That is a count of windows, not of failed calls. Most windows never get tested, because a small department expects well under one call in any four hours. The number being predicted is exposure, not outcome: how often the station could not have answered, whether or not anybody rang.

| | Roster check | Turnout | 
|---|---|---|
| Real failures caught | 129 of 914 | **446 of 914** |
| Recall | 14.1% | **48.8%** |
| Failures it called fine | **785** | 468 |
| Precision | 100.0% | 85.8% |

**Turnout catches 3.5 times as many as the roster check: 446 against 129.** Of the 785 exposed windows the roster check calls covered, Turnout catches 317. Those are the windows where enough people are on the board and not enough of them come.

The roster check is not merely less sensitive. It is wrong in the one direction that matters, because the window it calls covered is the window nobody looks at again.

Turnout pays for that with lower precision: it flags windows that would have held. That trade is deliberate and is the reason the interrupt budget exists in code. A flagged window that holds costs one text message. A missed one costs a response.

Ground truth is simulated, because nobody publishes per-member turnout records for a real department. Each window is drawn once rather than averaged, the per-member probabilities come from the same Beta posterior the product uses, and the seed is fixed at `20260914` so the number is reproducible rather than the best of several runs.
<!-- baseline:end -->

<!-- wild:start -->
## The same parser on messages we did not write

Generated 2026-09-09 22:43 UTC by `python -m evals.wild_eval`.

The 82 phrasings above are ones I wrote. That measures whether the parser handles the cases I thought of, which is worth knowing and is not the same as knowing what it does with a message I did not imagine.

So: the same parser, over the first **500 messages** of the [UCI SMS Spam Collection](https://archive.ics.uci.edu/dataset/228/sms+spam+collection), 5,574 real text messages gathered for academic research years before this project existed. Not one is a reply to a coverage poll. Taken in corpus order, nothing discarded after being read.

The parser's job is not to have an opinion about every message. It is to be certain or to defer: read the unambiguous answers without a model call, and hand everything else to the model rather than guessing. A wrong reading puts somebody on the board who is not coming.

**490 of 500 were correctly deferred, 98.0%.** The rule parser declined to interpret them and passed them on rather than inventing an answer.

Of the 10 it did read, **2 are bare affirmatives or negatives** such as "Ok", "Yes" or "No". Those are not mistakes: as the answer to a yes or no question, that is exactly what they mean, and reading them without a model call is the whole point of having a rule parser at all.

That leaves **8 in 500 messages**, 1.60%. Every one, so you can judge them rather than take the number on trust:

| What arrived | Read as |
|---|---|
| No calls..messages..missed calls | no |
| Yup next stop. | yes |
| Dear good morning now only i am up | partial |
| Tomarrow final hearing on my laptop case so i cant. | no |
| Kallis wont bat in 2nd innings. | no |
| No calls..messages..missed calls | no |
| Nah can't help you there, I've never had an iphone | no |
| Morning only i can ok. | partial |

Several of those are defensible rather than wrong. "Tomarrow final hearing on my laptop case so i cant" read as no, which is what the person is saying. So is "Nah can't help you there". The genuinely bad one is cricket commentary containing the word "wont". Read as answers to "can you cover Thursday, reply Y or N", most of these are the reading a person would give.

This number was worse before it was measured. The first run of this file deferred 92.6 percent and misread 35, because the heuristics matched substrings anywhere in a message of any length: "Goodmorning sleeping ga." read as available until noon, and "I can't even move. Pain is killing me." read as yes, because it contains "i can". Two changes fixed it. A length guard, since the longest genuine reply in the entire test suite is four words and a heuristic over a long message is a guess. And whole word matching instead of substrings. Neither would have been found without a case set somebody else wrote.

| Intent | Count |
|---|---|
| unknown | 490 |
| no | 5 |
| yes | 3 |
| partial | 2 |

Deterministic and model-free, so it runs in CI and the figure cannot drift with a model version. Download the corpus with `python -m evals.wild_eval --fetch`. It is published by the UCI Machine Learning Repository at [https://archive.ics.uci.edu/dataset/228/sms+spam+collection](https://archive.ics.uci.edu/dataset/228/sms+spam+collection) and is not redistributed here.
<!-- wild:end -->

<!-- refusal:start -->
## What it refuses to do

Generated 2026-09-09 19:32 UTC by `python -m evals.refusal_eval`.

An agent that asks unpaid people for hours of their life, and asks a neighbouring department to commit an apparatus, is defined as much by what it will not do as by what it will. Every refusal below is enforced in code, not in a prompt.

**14 of 14 adversarial cases refused. 0 false refusals across 6 legitimate cases.**

The second number is the one that matters. Refusing everything would score 100 percent on the first. A false refusal is worse than the thing it was guarding against: a coverage window that is wrongly declined stays open with nobody looking at it again.

### Refused, correctly

| Area | What was attempted | What happened |
|---|---|---|
| Mutual aid identity | unsigned request | unsigned: no mutual aid signature on the request |
| Mutual aid identity | request signed by another department | signed by cedar but the request says it came from millbrook |
| Mutual aid identity | request widened after signing | signature does not match the contents of the request from millbrook |
| Mutual aid identity | malformed signature | malformed signature: not-a-signature |
| Mutual aid identity | request naming a department with no agreement on file | no mutual aid agreement on file with stranger |
| Contact policy | text to a member who sent STOP | Dana Ortiz opted out (STOP). Policy: never text an opted-out member. |
| Contact policy | ask beyond the weekly limit | Luis Reyes has already been asked 2 times this week (limit 2). Policy: pick someone else or move on. |
| Contact policy | text to a member id that does not exist | unknown member id |
| Quiet hours | ask sent at 22:30, inside quiet hours | held until 2026-09-10T07:00:00 |
| Scoring endpoint | role that does not exist | HTTP 400 'astronaut' is not a valid Role |
| Scoring endpoint | probability above one | HTTP 400 person 0: responds must be between 0 and 1 |
| Scoring endpoint | nobody available at all | HTTP 400 available must be a non-empty list of people who could respond |
| Scoring endpoint | more people than it will score | HTTP 400 that is more people than this endpoint will score in one go |
| Chief's attention | 4 decisions on one day, budget 3 | 3 sent, 1 deferred |

### Allowed, correctly

| Area | What was attempted | What happened |
|---|---|---|
| Mutual aid identity | correctly signed request from a real peer | verified as millbrook |
| Contact policy | ask a member inside their limits | allowed |
| Contact policy | tell a capped member the window is covered | allowed |
| Quiet hours | ask sent at 10:00, outside quiet hours | sent |
| Scoring endpoint | a valid window with three people | HTTP 200 |
| Chief's attention | the most urgent decision of the day | 3 sent |

Rerun with `python -m evals.refusal_eval`. It calls no model, so it is deterministic and runs in CI on every push. The same cases run over real HTTP between separate agents in `tests/test_a2a.py`.
<!-- refusal:end -->

## Reply parsing

Members answer a poll however they like. The rule parser handles the common cases without a model
call, so a member's reply is acknowledged in seconds rather than after a round trip. The model is
asked only when this returns `unknown`.

**82 phrasings, 100.0% read correctly.**

| Intent | Phrasings covered |
|---|---|
| yes | Y, yes, yep, yeah, yup, sure, ok, can do, in, i can, count me in, 10-4, roger, affirmative, available |
| no | N, no, nope, nah, can't, cannot, out, not available, negative, busy, working, not this week, out of town, on vacation, and free text such as "sorry, can't, at the plant all day" |
| partial | till 2, until 2pm, til noon, before 11, morning only, afternoon, after 1, from 3pm, and the same inside a longer sentence |
| stop, start, help, limits, status, gaps | the keyword forms, case insensitive |
| decision | 1, 2, 3, 2a, 2b, UNDO |
| unknown | anything it cannot read, which is escalated to the model rather than guessed |

A bare number under 7 in a partial window is read as the afternoon, because "till 2" in a daytime
coverage question never means two in the morning.

## Risk engine and crew feasibility

Exact and deterministic, so they are unit tested rather than evaluated. `pytest -q` runs 91 tests.

| Property | How it is checked |
|---|---|
| Hand-computed probability | One driver at 0.5 and two firefighters at 1.0 must give exactly 0.5 |
| Monotonic in members | Adding an available member never raises the risk score |
| Monotonic in hazard | A weather alert never lowers the risk score |
| Level boundaries | Each of the four thresholds is asserted |
| Crew matching | One person who is both driver and firefighter cannot fill two slots |
| Thin history | Under 90 days, the explanation says the estimate leans on a national pattern |

## Message length

Every template is rendered and length-checked in the test suite: member messages stay under 160
characters so they never split, chief decision messages under 300.

## Agent-to-Agent

Five tests run real A2A over HTTP between separate department servers, including one that asks a
peer for its roster and asserts that no roster comes back.
